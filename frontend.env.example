# Nur relevant für lokale Entwicklung außerhalb von Docker (npm run dev).
# Im Docker-Setup wird dieser Wert stattdessen als Build-Arg in docker-compose.yml gesetzt.
NEXT_PUBLIC_API_URL=http://localhost:8000
